{
  "name": "node-express-intro",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js" //script that 'types' node app.js into the terminal when we instead type npm start
  },
  "author": "Luke Schlangen",
  "license": "ISC",
  "dependencies": { //code is dependent on the two packages/modules below
    "body-parser": "^1.16.1", 
    "express": "^4.14.1"
  }
}
